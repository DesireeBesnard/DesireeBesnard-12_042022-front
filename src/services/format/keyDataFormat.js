/**
 * Formats data for Nutrients component
 * @param { object } data 
 * @returns { object }
 */
 export const formatKeyData = data => {
    let formatData = {}
    formatData.data = data
    return formatData
}
